export enum ConceptPath {
  ROUTING = 'routing',
  COMPONENT = 'component-concept',
  DI = 'di',
  FORM = 'form',
}

export enum ConceptName {
  ROUTING = 'ROUTING',
  COMPONENT = 'COMPONENT',
  DI = 'DI',
  FORM = 'FORMS',
}
